// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.msm.thepainline"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
